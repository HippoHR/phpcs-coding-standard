{
  "name": "hippohr/phpcs-coding-standard",
  "description": "",
  "scripts": {
    "pre-test": [
      "phpcs --config-set installed_paths ../../../Hippo"
    ],
    "test": [
      "@composer pre-test",
      "phpunit --filter \"Hippo\""
    ]
  },
  "config": {
    "sort-packages": true
  },
  "require": {
    "slevomat/coding-standard": "6.0.0"
  },
  "require-dev": {
    "phpunit/phpunit": "7.5.12",
    "squizlabs/php_codesniffer": "3.5.3"
  }
}
